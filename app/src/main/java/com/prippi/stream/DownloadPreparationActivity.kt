package com.prippi.stream

/**
 * Usa lo stesso motore di risoluzione del player, ma con una finestra dichiarata
 * trasparente nel manifest così Android mantiene visibile la schermata episodi.
 */
class DownloadPreparationActivity : PlayerActivity()
